---
title: "ioslides"
output:
  ioslides_presentation:
    incremental: true
    transition: default
    logo: Mydog.jpg
author: "寺井 雅人"
date: "Last Update(JST): `r format(Sys.time(), '%Y-%m-%d %X')`"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{=html}         
<style>
  slides > slide {
    overflow-x: auto !important;
    overflow-y: auto !important;
}
</style>
```

# セクション見出し（ # ）

## スライドのタイトル（ ## ）

### マスターテキスト（ ### ）

## 簡単なアニメーション
- Incremental Bullet
- Bulletポイントごとに表示します。

## test
```{r}
print(1 + 2)
```


## 水平スクロール

```{r, echo = F, warning=FALSE, message=FALSE}
library(kableExtra)
library(tidyverse)

mpg_list <- split(mtcars$mpg, mtcars$cyl)
disp_list <- split(mtcars$disp, mtcars$cyl)
inline_plot <- data.frame(gspec = mtcars$qsec, mpg_box = "", mpg_hist = "",
                          mpg_line1 = "", mpg_line2 = "",
                          mpg_points1 = "", mpg_points2 = "", mpg_poly = "")
inline_plot %>%
  kbl(booktabs = TRUE, caption = "Table 1 Summary of Mtcars") %>%
  kable_paper(full_width = FALSE) %>%
  column_spec(2, image = spec_boxplot(mpg_list)) %>%
  column_spec(3, image = spec_hist(mpg_list)) %>%
  column_spec(4, image = spec_plot(mpg_list, same_lim = TRUE)) %>%
  column_spec(5, image = spec_plot(mpg_list, same_lim = FALSE)) %>%
  column_spec(6, image = spec_plot(mpg_list, type = "p")) %>%
  column_spec(7, image = spec_plot(mpg_list, disp_list, type = "p")) %>%
  column_spec(8, image = spec_plot(mpg_list, polymin = 5))

```


## 上下左右スクロール
![](C:/Users/terai-masato/Documents/ioslides_JapanR_2024/mydog.jpg)

## 二段組も可能
<div class="columns-2">
HTMLで記載すればできます
HTMLで記載すればできます
HTMLで記載すればできます
HTMLで記載すればできます
HTMLで記載すればできます
HTMLで記載すればできます
</div>


## Getting up | What I like to do first thing